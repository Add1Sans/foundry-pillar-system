import { Archetype } from '@/types';

export default function ArchetypeCard({ archetype }: { archetype: Archetype }) {
  return (
    <div className="card">
      <div className="font-semibold">{archetype.name}</div>
      <div className="text-xs text-zinc-400 mb-2">{archetype.primary} • {archetype.identity}</div>
      <div className="text-sm">
        <div className="font-semibold">Signature Mechanics</div>
        <ul className="list-disc pl-5">
          {archetype.signatureMechanics.map((m,i)=><li key={i}>{m}</li>)}
        </ul>
      </div>
    </div>
  );
}
